{
    "name": "team-22-root-build",
    "version": "1.0.0",
    "description": "Root build script for Vercel deployment",
    "scripts": {
        "vercel-build": "cd frontend/web && npm install && npm run build"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}