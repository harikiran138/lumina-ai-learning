# Deploy Directory

This folder is the canonical host-side deployment asset directory.

- `deploy.sh`: host update/deploy helper
- `nginx/`: nginx site and proxy configuration

Use this folder for operational deployment assets that are consumed directly on a host or VM.
