name: lumina_mobile
description: A new Flutter project for Lumina AI Learning.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^6.0.5

  # Networking
  http: ^1.1.0

  # UI & Utilities
  google_fonts: ^6.1.0
  flutter_markdown: ^0.6.14
  go_router: ^12.0.0
  intl: ^0.18.1
  lucide_icons: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
