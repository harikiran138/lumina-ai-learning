# Deployment Directory

This folder contains cloud-environment deployment automation.

- `aws/`: AWS provisioning scripts, compose variants, and bootstrap helpers

Use `deployment/` for cloud-specific automation. Use `deploy/` for canonical host-side deployment assets.
