import json
import os

def generate_doc():
    # Load the results from a temp file or just run the script again and capture output
    # For now, I'll assume I have the JSON structure from the previous command status
    # Since I can't easily pipe command output to this python script here, I'll recreate the fetch logic briefly or just write the processing script.
    
    pass

# Instead of a script here, I will just manually format the core ones and provide a summary for the rest, 
# as listing 273 students in a response is too much. 
# But the user wants a DOC. I will create a file.

# I'll create a script that generates the file.

with open('nsrit_logins.json', 'w') as f:
    # I will put the JSON I got from the command output here (conceptually)
    # But I have to actually get it. 
    pass
