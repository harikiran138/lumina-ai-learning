[bandit]
# Exclude seed/utility scripts that are not production code
exclude_dirs = backend/tmp

# Skip checks that are intentional patterns in this ML/LMS project:
#   B608 - SQL string construction in seed/migration scripts (not user-input driven)
#   B615 - HuggingFace from_pretrained without revision pinning (required for ML services)
#   B104 - Binding to 0.0.0.0 is intentional in Docker/container deployments
skips = B608,B615,B104
